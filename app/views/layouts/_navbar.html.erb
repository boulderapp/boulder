<nav class="bg-gray-800">
  <div class="mx-auto max-w-7xl px-2 sm:px-6 lg:px-8">
    <div class="w-full h-16 flex items-center">
      <%= link_to  "Campaigns", campaigns_url, class: "bg-gray-300 rounded mx-2 px-2" %>
      <div class="grow"></div>
      <div class="flex">
        <% if logged_in? %>
          <%= link_to "Profile", user_url(current_user), class: "bg-blue-300 rounded mx-2 px-2" %>
          <%= button_to "Logout", logout_url, class: "bg-red-300 rounded mx-2 px-2", method: :post %>
        <% else %>
          <%= link_to "Login", login_url %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
