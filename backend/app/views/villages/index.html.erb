<%= render partial: "shared/breadcrumbs", locals: { crumbs: [
    @campaign, 
    Crumb.new("Villages", campaign_villages_path(@campaign)),
] } %>
<div class="w-full">
  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Villages</h1>
    <%= link_to 'New village', new_campaign_village_path, class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium" %>
  </div>

  <div id="villages" class="min-w-full">
    <%= render @villages %>
  </div>
</div>
